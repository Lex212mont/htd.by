# ARCHIVE / MIRROR — do not edit

Canonical repository: **https://github.com/Lex212mont/htdi.by**

Cloudflare Pages project `htdi-news` still builds from this repo (`master`) until the Cloudflare GitHub App is granted access to `htdi.by`.

To deploy: from `htdi.by` run `./scripts/deploy-htdi.sh` (pushes + mirrors here).
